scan:
  vuln-type:
    - os
    - library
  scanners:
    - vuln
    - config
    - secret

report:
  format: table
  exit-code: 1
  severity:
    - HIGH
    - CRITICAL

vulnerability:
  type:
    - os
    - library
  ignore-unfixed: true